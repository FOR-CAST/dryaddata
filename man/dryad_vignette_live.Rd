% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vignette.R
\name{dryad_vignette_live}
\alias{dryad_vignette_live}
\title{Vignette-build helper}
\usage{
dryad_vignette_live(require_credentials = FALSE)
}
\arguments{
\item{require_credentials}{Logical. If \code{TRUE}, additionally requires
OAuth credentials to be available (the downloading vignette uses this).}
}
\value{
A single logical.
}
\description{
Internal hook used by the package's own vignettes (and downstream
packages that want the same convention). Returns \code{TRUE} when calling
chunks should evaluate against the live Dryad API and \code{FALSE} otherwise.
}
\details{
A chunk is "live" when:
\itemize{
\item \code{DRYADDATA_BUILD_LIVE_VIGNETTES = "true"} is set in the build
environment (opt-in; keeps R CMD check fast and deterministic), AND
\item \code{require_credentials = FALSE}, OR \code{\link[=dryad_has_credentials]{dryad_has_credentials()}} reports
\code{TRUE}.
}

As a side effect, when the result is \code{TRUE} the on-disk cache is
redirected to a fresh \code{tempfile()} so the vignette build never writes
to the user's persistent cache.
}
\keyword{internal}
