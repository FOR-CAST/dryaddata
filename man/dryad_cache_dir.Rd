% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{dryad_cache_dir}
\alias{dryad_cache_dir}
\alias{dryad_cache_list}
\alias{dryad_cache_clear}
\alias{dryad_cache_info}
\title{Cache location and maintenance}
\usage{
dryad_cache_dir(path = NULL)

dryad_cache_list(
  what = c("all", "http", "downloads", "datasets", "versions", "files")
)

dryad_cache_clear(
  what = c("all", "http", "downloads", "datasets", "versions", "files"),
  files = NULL
)

dryad_cache_info()
}
\arguments{
\item{path}{When supplied to \code{dryad_cache_dir()}, sets the active cache
directory (creating it if needed) and returns the path invisibly.}

\item{what}{Which subtree to inspect or clear. One of \code{"all"}
(default), \code{"http"}, \code{"downloads"}, \code{"datasets"}, \code{"versions"},
\code{"files"}.}

\item{files}{Optional character vector of file paths to delete from
the cache (typically taken from \code{dryad_cache_list()$path}). Paths
outside \code{dryad_cache_dir()} are silently ignored.}
}
\value{
\itemize{
\item \code{dryad_cache_dir()}: a character scalar (the cache root).
\item \code{dryad_cache_info()}: a data frame: \code{subtree}, \code{files}, \code{bytes}.
\item \code{dryad_cache_list()}: a data frame: \code{path}, \code{subtree}, \code{bytes},
\code{mtime}. Empty (0 rows) when nothing is cached.
\item \code{dryad_cache_clear()}: the number of files deleted, invisibly.
}
}
\description{
\code{dryad_cache_dir()} returns (or sets) the on-disk directory the package
uses to cache HTTP responses (under \verb{http/}) and downloaded files
(under \verb{downloads/\{datasets,versions,files\}/}). The default is
\code{tools::R_user_dir("dryaddata", "cache")}.
}
\details{
Lookup precedence: explicit argument > \code{DRYADDATA_CACHE_DIR} env var >
\code{dryaddata.cache_dir} option > default.

\code{dryad_cache_info()} returns a per-subtree summary (file count and
total bytes), useful for a quick "how big is my cache?" check.

\code{dryad_cache_list()} returns a data frame with one row per cached
file (path, subtree, size, mtime). Filter by subtree and use the
returned \code{path} column to inspect, copy, or selectively delete files.

\code{dryad_cache_clear()} deletes cached files. By default it clears every
subtree. Pass \code{what} to target one (\code{"http"}, \code{"downloads"}, or one of
the download subtypes \code{"datasets"} / \code{"versions"} / \code{"files"}), or
pass \verb{files = <character vector>} to remove specific paths. The
\code{files} argument is intersected with the cache root, so a path
outside the cache is silently ignored: \code{dryad_cache_clear()} can
never delete a file outside \code{dryad_cache_dir()}.
}
\examples{
withr::local_envvar(DRYADDATA_CACHE_DIR = tempfile("dryadcache"))
dryad_cache_dir()
dryad_cache_info()

\dontrun{
# After downloading a few things:
inv <- dryad_cache_list("datasets")
inv[order(-inv$bytes), ]               # biggest first

# Drop just the largest cached archive:
dryad_cache_clear(files = inv$path[which.max(inv$bytes)])

# Or clear an entire subtree:
dryad_cache_clear("http")
}

dryad_cache_clear()
}
