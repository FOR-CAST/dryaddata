% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/datasets.R
\name{dryad_datasets}
\alias{dryad_datasets}
\title{List Dryad datasets}
\usage{
dryad_datasets(
  publication_issn = NULL,
  publication_name = NULL,
  manuscript_number = NULL,
  curation_status = NULL,
  per_page = 20L,
  page = 1L,
  all_pages = FALSE,
  max_pages = Inf
)
}
\arguments{
\item{publication_issn, publication_name, manuscript_number, curation_status}{Optional filters. See the
\href{https://datadryad.org/api}{API docs} for accepted values
(e.g. \code{curation_status} is one of \code{"In progress"}, \code{"Curation"},
\code{"Published"}, ...).}

\item{per_page}{Results per page (max 100).}

\item{page}{Page number (1-indexed). Ignored when \code{all_pages = TRUE}.}

\item{all_pages}{If \code{TRUE}, automatically follow \verb{_links.next} and combine
results. Use with care for large queries.}

\item{max_pages}{Hard cap when \code{all_pages = TRUE}. Default \code{Inf}.}
}
\value{
A list with three elements:
\itemize{
\item \code{metadata}: \code{count}, \code{total}, HAL \code{links}, and (when paginating)
the number of \code{pages} fetched.
\item \code{records}: the raw list of dataset objects exactly as returned by
the API (handy for nested fields like authors or \verb{_links}).
\item \code{data}: a data frame stacking the scalar fields, with nested fields
as list-columns.
}
}
\description{
Wraps \code{GET /datasets}. Supports the optional filter parameters documented
by the API and HAL-style pagination.
}
\examples{
\dontrun{
dryad_use_sandbox()
dryad_datasets(per_page = 3)
}
}
