% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{dryad_auth}
\alias{dryad_auth}
\title{Authenticate against the Dryad API}
\usage{
dryad_auth(client_id = NULL, client_secret = NULL, force = FALSE)
}
\arguments{
\item{client_id, client_secret}{OAuth2 application credentials. When \code{NULL}
(the default), the package looks them up from keyring then environment
variables as described above. Supply directly to override.}

\item{force}{If \code{TRUE}, ignore any cached token and request a fresh one.}
}
\value{
Invisibly, a character scalar containing the access token.
}
\description{
Acquires an OAuth2 access token using the \emph{client credentials} grant and
caches it for the rest of the session. Subsequent calls reuse the cached
token until it nears expiry, at which point it is refreshed transparently.
}
\details{
Credentials are looked up in this order:
\enumerate{
\item Explicit \code{client_id} / \code{client_secret} arguments.
\item The \href{https://r-lib.github.io/keyring/}{keyring} package, under service
\code{"dryaddata"} and usernames \code{"client_id"} / \code{"client_secret"}. This is
the preferred storage: secrets live in the OS keychain rather than in
a plain-text file on disk. See \code{\link[=dryad_set_credentials]{dryad_set_credentials()}}.
\item Environment variables \code{DRYAD_CLIENT_ID} / \code{DRYAD_CLIENT_SECRET},
typically set in \verb{~/.Renviron}. Avoid \code{Sys.setenv()} in scripts;
those calls land in your command history.
}

The token endpoint is taken from \code{\link[=dryad_token_url]{dryad_token_url()}}, which respects
\code{\link[=dryad_use_sandbox]{dryad_use_sandbox()}}.

You usually do not need to call this function directly: any function that
requires authentication (notably the \verb{dryad_download_*()} family) will
call it as needed.
}
\examples{
\dontrun{
dryad_use_sandbox()
dryad_auth() # uses keyring or env vars
}
}
\seealso{
\code{\link[=dryad_set_credentials]{dryad_set_credentials()}}, \code{\link[=dryad_has_credentials]{dryad_has_credentials()}},
\code{\link[=dryad_clear_token]{dryad_clear_token()}}
}
