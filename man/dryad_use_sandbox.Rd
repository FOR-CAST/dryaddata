% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{dryad_use_sandbox}
\alias{dryad_use_sandbox}
\title{Switch between Dryad's sandbox and production hosts}
\usage{
dryad_use_sandbox(sandbox = TRUE)
}
\arguments{
\item{sandbox}{Logical. \code{TRUE} (default) switches the active host to the
sandbox (\verb{https://sandbox.datadryad.org}); \code{FALSE} switches back to
production (\verb{https://datadryad.org}).}
}
\value{
Invisibly, a named list with the \emph{previous} \code{base_url} and
\code{token_url}, suitable for restoring later via \code{\link[=dryad_base_url]{dryad_base_url()}} and
\code{\link[=dryad_token_url]{dryad_token_url()}}.
}
\description{
Convenience wrapper that points \code{\link[=dryad_base_url]{dryad_base_url()}} and \code{\link[=dryad_token_url]{dryad_token_url()}}
at Dryad's public sandbox or production deployment in a single call. The
sandbox accepts independent credentials and is the right target for
development; production is the default.
}
\examples{
old <- dryad_use_sandbox()
dryad_base_url() # https://sandbox.datadryad.org/api/v2
dryad_use_sandbox(FALSE)
dryad_base_url() # https://datadryad.org/api/v2
# Restore prior values:
dryad_base_url(old$base_url)
dryad_token_url(old$token_url)
}
