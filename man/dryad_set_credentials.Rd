% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{dryad_set_credentials}
\alias{dryad_set_credentials}
\alias{dryad_clear_credentials}
\alias{dryad_has_credentials}
\alias{dryad_clear_token}
\title{Store, check, and clear Dryad credentials}
\usage{
dryad_set_credentials(client_id = NULL, client_secret = NULL)

dryad_clear_credentials()

dryad_has_credentials()

dryad_clear_token()
}
\arguments{
\item{client_id, client_secret}{OAuth2 application credentials, as strings.
If either is \code{NULL}, you will be prompted interactively.}
}
\value{
\code{dryad_set_credentials()} and \code{dryad_clear_credentials()} return
\code{NULL} invisibly. \code{dryad_has_credentials()} returns \code{TRUE} / \code{FALSE}.
\code{dryad_clear_token()} returns \code{NULL} invisibly.
}
\description{
\code{dryad_set_credentials()} saves your OAuth2 application's \code{client_id} and
\code{client_secret} in the OS keychain via the
\href{https://r-lib.github.io/keyring/}{keyring} package. This is the preferred
way to make credentials available to \code{\link[=dryad_auth]{dryad_auth()}}: secrets live in the
OS keychain rather than in a plain-text file, and they are unavailable to
other R sessions until the keyring is unlocked.
}
\details{
\code{dryad_has_credentials()} reports whether credentials are available
(either in keyring or in environment variables). It does \strong{not} unlock
or read any secret value, so it never prompts.

\code{dryad_clear_token()} discards any cached access token; the next
authenticated call will request a new one. \code{dryad_clear_credentials()}
removes the stored credentials from the OS keychain.
}
\examples{
dryad_has_credentials()
dryad_clear_token()

\dontrun{
# Interactive; prompts for the values:
dryad_set_credentials()

# Or non-interactive:
dryad_set_credentials("my-client-id", "my-client-secret")

dryad_clear_credentials()
}
}
\seealso{
\code{\link[=dryad_auth]{dryad_auth()}}
}
