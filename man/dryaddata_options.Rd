% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{dryaddata_options}
\alias{dryaddata_options}
\alias{dryaddata-options}
\title{Options and environment variables used by dryaddata}
\description{
Reference page that lists every R option and environment variable the
package consults, alongside their defaults and accessor functions.
}
\section{Options and environment variables}{

\code{dryaddata}'s behaviour is configured through R options and environment
variables. Lookup precedence is \strong{explicit function argument > environment
variable > R option > hard-coded default}. The OAuth credentials follow
a separate path described in \code{\link[=dryad_auth]{dryad_auth()}}.

\describe{
\item{\strong{API base URL}}{
Option \code{dryaddata.base_url}; env var \code{DRYAD_BASE_URL}. Default
\code{"https://datadryad.org/api/v2"}. Toggle to the sandbox via
\code{\link[=dryad_use_sandbox]{dryad_use_sandbox()}} or set directly with \code{\link[=dryad_base_url]{dryad_base_url()}}.
}
\item{\strong{OAuth token URL}}{
Option \code{dryaddata.token_url}; env var \code{DRYAD_TOKEN_URL}. Default
\code{"https://datadryad.org/oauth/token"}. Used by \code{\link[=dryad_auth]{dryad_auth()}}.
}
\item{\strong{Cache directory}}{
Option \code{dryaddata.cache_dir}; env var \code{DRYADDATA_CACHE_DIR}. Default
\code{tools::R_user_dir("dryaddata", "cache")}. Holds both the HTTP
response cache (\verb{<cache_dir>/http/}) and downloaded files
(\verb{<cache_dir>/downloads/}). See \code{\link[=dryad_cache_dir]{dryad_cache_dir()}}.
}
\item{\strong{Disable keyring}}{
Option \code{dryaddata.disable_keyring} (logical; default \code{FALSE}). When
\code{TRUE}, the credential resolver skips the keyring branch entirely
and only consults \code{DRYAD_CLIENT_ID} / \code{DRYAD_CLIENT_SECRET}. Useful
in tests and CI; the package test suite sets this automatically.
}
\item{\strong{OAuth client ID} (secret)}{
Preferred: store in the OS keychain via \code{\link[=dryad_set_credentials]{dryad_set_credentials()}}
under service \code{"dryaddata"}, username \code{"client_id"}. Fallback: env
var \code{DRYAD_CLIENT_ID} in \verb{~/.Renviron}. No R option (so the value
is never recorded in plain text on disk).
}
\item{\strong{OAuth client secret} (secret)}{
Preferred: store in the OS keychain via \code{\link[=dryad_set_credentials]{dryad_set_credentials()}}
under service \code{"dryaddata"}, username \code{"client_secret"}. Fallback:
env var \code{DRYAD_CLIENT_SECRET} in \verb{~/.Renviron}. No R option.
}
}

Avoid setting secrets with \code{Sys.setenv()} from a script: the call lands
in your command history and any saved \code{.Rhistory}. Use
\code{\link[=dryad_set_credentials]{dryad_set_credentials()}} or \verb{~/.Renviron} instead.
}

\seealso{
\code{\link[=dryad_base_url]{dryad_base_url()}}, \code{\link[=dryad_token_url]{dryad_token_url()}}, \code{\link[=dryad_use_sandbox]{dryad_use_sandbox()}},
\code{\link[=dryad_cache_dir]{dryad_cache_dir()}}, \code{\link[=dryad_auth]{dryad_auth()}}, \code{\link[=dryad_set_credentials]{dryad_set_credentials()}}
}
